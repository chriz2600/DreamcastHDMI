#!/bin/bash -l

source /root/build.projects

if [ -z "$1" ] ; then
    echo
    echo "usage: "$(basename $0)" <.qpf>"
    echo
    exit 1
fi

project=$(echo $1 | sed -e 's:\.qpf::g')
if [ ! -e $project.qpf -a -e /build/$project.qpf ] ; then
    cd /build
fi
doBuildProject
