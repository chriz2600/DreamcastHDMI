
firmware:
  stage: build
  script: /root/build
  artifacts:
    paths:
    - /srv/*.jic
    - /srv/*.rbf
  tags:
    - dreamcast-hdmi

publish:
  stage: deploy
  script: create-index-html
  dependencies: []
  tags:
    - index-gen
